---
title: Drop Filter
id: irregular_implements:drop_filter
related_items: [irregular_implements:drop_filter, irregular_implements:voiding_drop_filter]
type: item
---

The **Drop Filter** can hold an [Item Filter](@irregular_implements:item_filter), and will prevent items matching the filter from entering your inventory.

The **Voiding Drop Filter** does the same, but instead deletes the items matching the filter entirely.

<ProjectRecipe id="irregular_implements:drop_filter"/>
<ProjectRecipe id="irregular_implements:voiding_drop_filter"/>